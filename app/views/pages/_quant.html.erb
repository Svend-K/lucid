<div id="compare" class="page-size">
  <div class="page-wrapper container">
    <div class="title">
      <h1>
        How do <%= @current_city.name.capitalize %> and <%= @destination_city.name.capitalize %> compare?
      </h1>
    </div>
    <div class="row">
      <div class="col-xs-12 col-md-6">
        <div class="color content">
          <h3 class="font">😎 Quality of Life 😎</h3>
          <%= line_chart [
            {name: @current_city.name, data: @current_city_graph_lifequality},
            {name: @destination_city.name, data: @destination_city_graph_lifequality},
            ], colors: ['#76AD99','#F47C2F'],
            curve: false
            %>
        </div>
      </div>
      <div class="col-xs-12 col-md-6">
        <div class="color content">
          <h3 class="font">💵 Cost of Living 💵</h3>
          <%= line_chart [
            {name: @current_city.name, data: @current_city_graph_quantitative},
            {name: @destination_city.name, data: @destination_city_graph_quantitative}
            ],  colors: ['#76AD99','#F47C2F'],
            curve: false
            %>
        </div>
      </div>
    </div>
    <p>
      <% if @spending_in_current_city && @spending_in_dest_city %>
        <p>
        In average, you need <strong>€<%=@spending_in_current_city.round(0)%></strong> to live in <%= @current_city.name.capitalize %> and <strong>€<%=@spending_in_dest_city.round(0)%></strong> to live in <%= @destination_city.name.capitalize %>
        </p>
      <% end %>
    </p>
  </div>

  <a href="#facts" class="scrolldown-button">
    <i class="fa fa-chevron-down" aria-hidden="true"></i>
  </a>
</div>
